for segment in my_snake.turtles[:-3]:
    #     if my_snake.head.distance(segment)<10:
    #         game_on=False
    #         time.sleep(1)
    #         window.clear()
    #         score.game_over()
